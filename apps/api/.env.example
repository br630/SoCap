# =============================================================================
# Server Configuration
# =============================================================================

# Node environment: 'development', 'production', or 'test'
# Determines logging level, error handling, and other environment-specific behavior
NODE_ENV=development

# Port number for the Express server to listen on
# Default: 3000
PORT=3000

# Base URL of the API (used for CORS, redirects, and generating absolute URLs)
# Example: http://localhost:3000 or https://api.yourdomain.com
API_URL=http://localhost:3000

# =============================================================================
# PostgreSQL Database Configuration
# =============================================================================

# PostgreSQL connection string (Prisma format)
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=SCHEMA
# Example: postgresql://user:password@localhost:5432/socap_db?schema=public
DATABASE_URL=postgresql://user:password@localhost:5432/socap_db?schema=public

# =============================================================================
# JWT Authentication Configuration
# =============================================================================

# Secret key used to sign JWT tokens
# IMPORTANT: Use a strong, random string in production (min 32 characters)
# Generate with: openssl rand -base64 32
JWT_SECRET=your-super-secret-jwt-key-change-in-production

# Secret key for refreshing JWT tokens (should be different from JWT_SECRET)
# Generate with: openssl rand -base64 32
JWT_REFRESH_SECRET=your-super-secret-refresh-key-change-in-production

# Access token expiry time (e.g., '15m', '1h', '7d')
# Common values: '15m' (15 minutes) or '1h' (1 hour)
JWT_ACCESS_TOKEN_EXPIRY=15m

# Refresh token expiry time (e.g., '7d', '30d')
# Common values: '7d' (7 days) or '30d' (30 days)
JWT_REFRESH_TOKEN_EXPIRY=7d

# =============================================================================
# Firebase Admin SDK Configuration
# =============================================================================

# Firebase project ID
# Found in Firebase Console > Project Settings > General
FIREBASE_PROJECT_ID=your-firebase-project-id

# Firebase private key (from service account JSON)
# This is the private_key field from your Firebase service account JSON
# IMPORTANT: Keep the newlines and escape characters intact
# Format: "-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYOUR_PRIVATE_KEY_HERE\n-----END PRIVATE KEY-----\n"

# Firebase client email (from service account JSON)
# Format: firebase-adminsdk-xxxxx@your-project-id.iam.gserviceaccount.com
FIREBASE_CLIENT_EMAIL=firebase-adminsdk-xxxxx@your-project-id.iam.gserviceaccount.com

# Optional: Firebase database URL (if using Realtime Database)
# Format: https://your-project-id-default-rtdb.firebaseio.com
# FIREBASE_DATABASE_URL=https://your-project-id-default-rtdb.firebaseio.com

# =============================================================================
# OpenAI API Configuration
# =============================================================================

# OpenAI API key
# Get from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-openai-api-key-here

# OpenAI model to use for completions
# Common models: 'gpt-4', 'gpt-4-turbo', 'gpt-3.5-turbo'
OPENAI_MODEL=gpt-4

# Optional: OpenAI organization ID (if using organization-level API keys)
# OPENAI_ORG_ID=org-your-org-id

# =============================================================================
# Google APIs Configuration
# =============================================================================

# Google OAuth 2.0 Client ID
# Get from: https://console.cloud.google.com/apis/credentials
# Used for Google Sign-In and OAuth flows
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com

# Google OAuth 2.0 Client Secret
# Get from: https://console.cloud.google.com/apis/credentials
# Keep this secret and never commit it to version control
GOOGLE_CLIENT_SECRET=your-google-client-secret

# Google API Key (for services like Maps, Places, etc.)
# Get from: https://console.cloud.google.com/apis/credentials
# Restrict this key to specific APIs and domains in production
GOOGLE_API_KEY=your-google-api-key

# Optional: Google OAuth redirect URI
# Must match the URI configured in Google Cloud Console
# GOOGLE_REDIRECT_URI=http://localhost:3000/auth/google/callback

# =============================================================================
# Encryption Configuration
# =============================================================================

# AES encryption key for encrypting sensitive data
# Must be exactly 32 bytes (256 bits) for AES-256
# Generate with: openssl rand -hex 32
# IMPORTANT: Never change this after data has been encrypted, or you won't be able to decrypt it
AES_KEY=your-32-byte-hex-encryption-key-here-min-64-characters

# Optional: AES initialization vector (IV) - if not provided, will be generated per encryption
# Must be exactly 16 bytes (128 bits) for AES
# Generate with: openssl rand -hex 16
# AES_IV=your-16-byte-hex-iv-here

# =============================================================================
# Sentry Error Tracking Configuration
# =============================================================================

# Sentry DSN (Data Source Name) for error tracking
# Get from: https://sentry.io/settings/YOUR_ORG/projects/YOUR_PROJECT/keys/
# Format: https://xxxxx@xxxxx.ingest.sentry.io/xxxxx
SENTRY_DSN=https://xxxxx@xxxxx.ingest.sentry.io/xxxxx

# Sentry environment (e.g., 'development', 'staging', 'production')
# Helps filter errors by environment in Sentry dashboard
SENTRY_ENVIRONMENT=development

# Optional: Sentry release version (e.g., git commit hash or version number)
# SENTRY_RELEASE=1.0.0

# Optional: Sentry traces sample rate (0.0 to 1.0)
# Controls what percentage of transactions are sent to Sentry
# SENTRY_TRACES_SAMPLE_RATE=1.0

# =============================================================================
# Optional: Additional Configuration
# =============================================================================

# CORS allowed origins (comma-separated)
# Example: http://localhost:3000,https://yourdomain.com
# CORS_ORIGIN=http://localhost:3000

# Rate limiting configuration
# Maximum number of requests per window
# RATE_LIMIT_MAX=100

# Rate limiting window in milliseconds
# RATE_LIMIT_WINDOW_MS=900000

# Session secret (if using sessions)
# Generate with: openssl rand -base64 32
# SESSION_SECRET=your-session-secret-here

# Logging level: 'error', 'warn', 'info', 'debug'
# LOG_LEVEL=info
